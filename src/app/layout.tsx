import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Performance Tracking Tool (PTT) — Ministry of Health",
  description: "Ministry of Health Performance Tracking Tool for monitoring, tracking, verifying, and reporting activities across all Directorates and Units.",
  keywords: "Ministry of Health, Performance Tracking, PTT, Sierra Leone, Government",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full`}>
      <body className="min-h-full flex flex-col antialiased">{children}</body>
    </html>
  );
}
